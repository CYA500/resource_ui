name: resource_ui
description: "A Windows Resource Monitor App"
publish_to: 'none'

# هذا الجزء هو ما كان ينقصك وحل المشكلة
environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^5.1.1
  web_socket_channel: ^3.0.1
  glass_kit: ^3.0.0
  google_fonts: ^6.1.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
